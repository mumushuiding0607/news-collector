# 前端生成规范

## 原则

组件/模块只做一件事，跨模块调用通过接口。

## 规则

1. 基础设施（API、路由、状态、工具）只在一个模块负责
2. 同类模块必须放在同一目录，禁止不同抽象层次混放
3. 业务层调用基础设施时，必须调用对应模块，不自行实现
4. 超出 200 行拆分，但是禁止删除功能
   - 禁止为了满足 200 行限制而删除有用功能
   - 只有真正无用的内容才需要删除
   - 通过模块化、拆分、增加代码复用、整合重复逻辑来提升代码性能，从而满足 200 行限制
5. 副作用集中在外层
6. 组件通过 props/emit 或 store 通信，不直接依赖实现细节