## 新闻采集

采集分成三个模块： 

1. 采集列表，包括标题、发布日期(包含时分秒)和摘要，不访问链接对应的文章，将采集结果保存数据库，并记录采集的批次，采集批次不断递增；
2. 使用llm，依据`事件评估.md`提示词和新闻的标题和摘要判断哪些新闻可能有价值，并标记为有用；
3. 遍历最新批次且标记有用的新闻，根据它的url采集文章内容；


## 要求

1. 发布日期要包含年月日时分秒
2. 采集列表时若未发现发布日期，在采集新闻内容时要从中提取
3. 采集新闻内容时要尽可能的过滤掉无用的内容