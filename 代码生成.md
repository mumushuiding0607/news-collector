# 代码生成规范

## 原则

一个功能一个模块，业务逻辑层只组合调用，基础设施层统一实现，不重复。

## 规则

1. 基础设施逻辑（数据库、日志、缓存、邮件、配置、认证等）只在一个模块负责
2. 同类模块必须放在同一目录，禁止不同抽象层次混放
3. 业务逻辑层需要基础设施时，必须调用对应模块，不自行实现
4. 超出 200 行的文件必须拆分，但是禁止删除功能
   - 禁止为了满足 200 行限制而删除有用功能
   - 只有真正无用的内容才需要删除
   - 通过模块化、拆分、增加代码复用、整合重复逻辑来提升代码性能，从而满足 200 行限制
5. 函数单一职责，副作用集中在外层
6. 模块间通过接口通信，不依赖具体实现
7. 禁止重复逻辑，相同/相似逻辑必须提取到公共模块

## 生成时必须主动优化

发现已有代码违反上述规则时，当场重构，不只是指出问题。